version: 1
ruleset: views

cases:
  - rule: no-domain-logic
    name: View自身が採点規則を実装する
    fixture: fixtures/violation.tsx
    expected: violation

  - rule: no-domain-logic
    name: Viewが責務を接続して表示する
    fixture: fixtures/compliant.tsx
    expected: compliant
