version: 1
ruleset: game-session

cases:
  - rule: play-facts-only
    name: sessionが難易度分類を所有する
    fixture: fixtures/violation.ts
    expected: violation

  - rule: play-facts-only
    name: sessionが経過事実だけを返す
    fixture: fixtures/compliant.ts
    expected: compliant
