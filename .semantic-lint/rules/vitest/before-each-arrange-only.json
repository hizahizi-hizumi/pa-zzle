{
  "version": 1,
  "id": "vitest/before-each-arrange-only",
  "title": "beforeEachは事前準備だけを行う",
  "target": "file",
  "severity": "warning",
  "violationThreshold": 0.9,
  "include": [
    "frontend/**/*.test.{ts,tsx,mjs}"
  ],
  "exclude": [],
  "source": {
    "path": ".claude/rules/vitest.md",
    "section": "テスト構造 > Arrangeの分離方法"
  },
  "question": {
    "instructions": "Classify whether every beforeEach in the test file follows the project rule that beforeEach performs Arrange only and does not execute the behavior under test.",
    "criteria": {
      "violation": "A beforeEach executes an operation, user action, or behavior whose result is subsequently verified by a test. The behavior being tested should instead be executed in the individual test body.",
      "compliant": "beforeEach blocks only prepare initial state, test data, mocks, spies, dependencies, or initial rendering. The behavior under test is executed inside each test.",
      "not_applicable": "The file contains no beforeEach.",
      "insufficient_context": "A beforeEach calls helpers whose role cannot be determined from the available source, so it is unclear whether they perform setup or the behavior under test."
    }
  }
}
