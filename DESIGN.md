# pa-zzle デザイン

`DESIGN.md` は、`APP.md` と `GAME.md` で定義した体験を、視覚、情報階層、操作フィードバック、動きへ翻訳するためのデザイン正本である。
正確なデザイントークン値は `frontend/styles/globals.css` の Tailwind CSS `@theme` を正本とし、この文書へ重複して記載しない。

## 1. 正本と責務

デザイン判断は次の責務に分ける。

1. `APP.md`: pa-zzle というアプリ全体でどのような体験を提供するか。
2. `GAME.md`: 個々のゲームで共通して守るゲーム体験。
3. `DESIGN.md`: それらの体験をどう見せ、どう反応させるか。
4. `frontend/styles/globals.css` の `@theme`: 色、文字、余白、角丸、影、レイヤー、動きなどの正確な値。
5. 共通UI: 複数画面で同じ意味と振る舞いが確認できた表現の実装。
6. 各ゲーム配下: 盤面、駒、ゲーム固有色、形、動きなど、そのゲームの題材とルールに由来する表現。

個別画面の都合だけで共通表現を増やさない。一方、共通化を避けること自体も目的にせず、同じ意味が繰り返し確認できたときに共通化する。

現段階ではアクセシビリティをデザイン要件として扱わない。既に存在するブラウザ標準の意味付けや操作性を意図的に壊すことはしないが、アクセシビリティ対応を理由に新しい設計・実装範囲を増やさない。

## 2. 視覚人格

pa-zzle の共通UIは、**ミニマルで静か**である。

これは、画面全体を無個性にしたり、すべてを単純な記号へ置き換えたりすることを意味しない。

> **器はミニマルで静か。中に置かれるゲームは、そのゲーム自身の姿をしている。**

共通UIは必要以上に自己主張せず、ゲーム固有の形、色、動きへ主役を譲る。

プレイヤーが考えている間、UIは話しかけ続けない。常時動く装飾、過剰な説明、不要な強調を避ける。

一方で、操作された瞬間、状態が変化した瞬間、問題を解いた瞬間には、必要な強さで明確に反応する。

pa-zzle の静けさは「反応がないこと」ではなく、**何も起きていないときには静かで、意味のある変化が起きたときだけ表情を持つこと**を意味する。

## 3. 視覚的優先順位

何を最も強く見せるかは、画面の目的によって変える。

### ゲームを探すとき

ゲームそのものの違いを認識できる視覚表現を主役にする。ゲーム名と説明文だけで一覧を成立させない。

### プレイ中

盤面、問題、現在の操作対象を最も強く見せる。アプリ共通UI、ゲーム名、手数、時間、二次操作などは、必要な文脈を保ちながら視覚的に退かせる。

プレイ画面では共通ヘッダーや共通レイアウトを維持することより、ゲームへ十分な画面資源を渡すことを優先する。

### 結果確認

クリアしたこととプレイ結果を主役にし、その後に次の主要操作、詳細情報を置く。

結果は情報を報告するだけでなくプレイを報いる場なので、プレイ中に抑えていた色、動き、強調を必要に応じて解放してよい。

### 設定・補助操作

現在判断・変更している対象を明確にし、説明や補助情報で画面密度を上げすぎない。

## 4. 表現の強さ

画面全体を常に同じ強さでデザインしない。

- **平常時**: 静かにする。不要な色、動き、説明、装飾を増やさない。
- **操作時**: 明確に反応する。選択や状態変化が起きたことを迷わせない。
- **プレイ中**: ゲーム固有表現を最も強くし、共通UIを退かせる。
- **達成時**: プレイ中に抑えていた表現を解放し、結果を報酬として返す。

この強弱によって、単に情報量の少ないUIではなく、思考と達成に合わせて表情が変わるゲーム体験を作る。

## 5. 色

### 共通UI

共通UIは低彩度の色を基調とし、色数を増やすことで階層を作らない。余白、文字、境界、面の明度差を優先する。

共通UIでは、役割に応じた意味トークンを使う。

基底面と情報階層:

- `background` / `foreground`: 基底面と通常の情報
- `card` / `popover`: 情報をまとめる面、一時的に前面へ出る面
- `muted`: 補助面、補助情報
- `border` / `input`: 面や入力領域の境界

操作と現在状態:

- `primary` / `secondary`: 操作の優先順位
- `accent`: hover など一時的な操作反応
- `ring`: 選択やフォーカスなど、現在対象の輪郭強調

アプリ共通の意味状態:

- `success`: 処理や操作が成功したこと、ゲームをクリアしたことなど、成立・完了を示す
- `warning`: 続行前に注意が必要なことや、現在の進行を失う可能性などを示す
- `error`: 入力不備、処理失敗、アプリ上の異常など、解消が必要な問題を示す
- `destructive`: リセット、削除など、現在の状態を失わせる破壊的操作を示す

`error` と `destructive` は同じ色に見える場合があっても意味を分ける。エラーは起きた状態、`destructive` は利用者が選択する操作の性質であり、将来それぞれの表現を独立して変更できるようにする。

説明や一般的な情報通知は `foreground` / `muted` の情報階層で表し、青などの専用 `info` 色を基本とはしない。選択・現在地・フォーカスも意味状態ではなく操作状態なので、`ring` / `accent` / `primary` を使い分ける。無効状態は専用色を増やさず、既存色の強度を下げて表す。

`primary` はブランド色を意味しない。現時点でブランド色を先に固定しない。

### ゲーム固有色

パズルの識別色、盤面データから決まる色、駒や液体など題材に由来する色はゲーム固有表現として扱い、共通UIの意味トークンへ昇格させない。

アプリ共通の `success` / `warning` / `error` / `destructive` と、ゲーム内で識別のために使う同系色は、同じ意味として扱わない。共通意味とゲーム固有意味は名前と利用範囲を分離する。

ゲーム固有色は共通UIより強い彩度を持てる。ただしゲーム内で必要な識別や題材表現を超えて、背景や共通操作までゲーム色で塗り広げない。

### 達成・報酬

`success` は「成立・完了」を表すために使えるが、「高得点」「自己ベスト」「特によいプレイ」と同義にはしない。クリアした事実と、そのプレイをどれだけ強く称えるかは別の情報である。

報酬は、結果名、スコア、過去との比較、ゲーム固有表現、色、動きなどを組み合わせて返す。現時点では称賛を一つの色へ固定できるだけの仕様がないため、`achievement` のような単一の共通色トークンは置かない。

評価に応じた段階的な色や演出の仕様は、プレイ評価の仕様と実プレイから納得できる形が得られてから決める。

## 6. 背景と面

すべての情報をカードで囲わない。

面を分けるのは、情報や操作を一つのまとまりとして読む必要があるとき、または前後関係を示す必要があるときに限る。

プレイ領域は「カードの中のコンテンツ」であることを前提にしない。画面そのものをゲーム領域として使う方が `GAME.md` の原則に合う場合は、共通のカード表現を外す。

影は浮いていることや前後関係を補助するために使い、装飾目的で重ねない。境界と影を同時に強くして二重に目立たせない。

## 7. タイポグラフィ

共通UIの文字は、ゲームより前へ出るためではなく、情報階層を静かに示すために使う。

- 画面タイトル: 非プレイ画面の目的を示す。常に最大の要素にする必要はない。
- セクション見出し: 結果や情報のまとまりを示す。
- 本文: 判断に必要な内容を読むために使う。
- 補助情報: 説明、戻り導線、二次操作などに使う。
- メタ情報: 内部・診断寄りの情報や補助ラベルに使う。

プレイ中は大きなページタイトルを置くことより、盤面へ視線を残すことを優先する。

結果画面では、クリア、スコア、自己ベストなど、そのプレイの報酬となる情報に大きい文字を使える。

数字を比較する場面では桁の揃いを重視する。盤面上の数字、記号、文字はゲーム固有表現として別の判断を持てる。

## 8. 余白と密度

同じアプリでも、画面の目的によって密度を変える。

- ゲームを探す画面: 各ゲームの違いを見つけられるだけの余白を持つ。
- 開始条件を選ぶ画面: 判断しやすい間隔を持ち、説明を詰め込みすぎない。
- プレイ画面: 共通UIの余白より、盤面そのものへ画面資源を渡す。
- 結果画面: 達成情報と次の行動をまとまりとして読める密度にする。

共通画面の余白には `@theme` のトークンを使う。盤面のセル、ボトル、駒などゲームの描画やルールに由来する寸法はゲーム固有値としてよい。

## 9. 形・境界・影

角丸、境界、影は、それ自体をアプリの個性にしすぎない。

- 操作部品は同じ意味のもの同士で形を揃える。
- 面のまとまりには、必要なときだけ境界や背景差を使う。
- 強い影、強いぼかし、装飾目的のグラデーションを共通UIの基本表現にしない。
- ゲームオブジェクトの形状は共通UIの角丸ルールへ従わせず、題材と操作から決める。

具体的な角丸・境界・影の値は `@theme` を正本とする。

## 10. ゲームを表す視覚表現

ゲームを汎用アイコンだけで代表させない。

ゲーム選択などでゲームを視覚的に表す場合は、可能な限りそのゲームで実際に見る・触る対象を使う。ウォーターソートなら色水と容器、ナンプレなら盤面と数字のように、ゲーム固有の視覚言語を縮約して使う。

アイコンは操作や補助情報の意味を短く示す用途には使えるが、ゲームそのものを表現する代替物とは考えない。

説明文は視覚表現を補助するために使い、「何のゲームか」を文章だけで理解させる構成を基本にしない。

## 11. 操作状態とフィードバック

### 通常

操作可能な要素は、装飾を増やしすぎずに操作対象だと理解できる形にする。

### 選択

選択されたことを即座に返す。ゲーム内の選択状態は、ゲーム固有の盤面表現を優先する。

### 押下・成立

入力を受け付けたこと、操作が成立したことを短い変化で返す。共通UIで大きな拡縮や跳ねる動きを標準にしない。

### 無効・不成立

ゲーム上の不成立を事前にすべて無効化して、プレイヤーの判断を奪わない。操作後に不成立を返す必要があるゲームでは、そのゲームに合うフィードバックを使う。

アプリ上の実行不能状態は視覚的に弱めてよい。

### 注意

`warning` は、まだ失敗していないが続行前に注意が必要な状態へ使う。たとえば、現在の進行を失う可能性がある操作を実行する前の注意などである。破壊的操作そのものの色とは分ける。

### エラー

`error` は、入力不備、処理失敗、アプリ上の異常など、解消が必要な問題へ使う。ゲーム固有の失敗、不正解、非合法手を必ずアプリ共通の `error` へ統一する必要はない。

### 破壊的操作

`destructive` は、リセットや削除など、実行すると現在の状態を失わせる操作へ使う。エラー表示の代替にはしない。

### 成功・クリア

`success` は、処理や操作の成功、ゲームのクリアなど、成立・完了を示すアプリ共通の意味に使う。高評価や自己ベストなどの称賛の強さは `success` だけで表さない。

クリアは色だけではなく、結果名、結果情報、次の行動を含む結果構造として返す。報酬の強さは、そのゲームで何を褒めるかと整合させる。

## 12. 動き

動きは、状態変化を理解させ、ゲーム固有の操作感を作り、達成を返すために使う。

共通UIは平常時に常時動かさない。hover や表示のためだけに過剰な動きを足さず、意味のある変化へ表現を集中させる。

ゲーム内アニメーションは、そのゲームで何が起きたかを表すために独自の時間・動きを持てる。共通UIの遷移時間をゲーム内の物理表現へ強制しない。

プレイ中の演出は思考や次の操作を不必要に止めない。達成時はプレイ中より表現を強められる。

## 13. レスポンシブ設計

スマートフォンを基準に、情報量、操作順、プレイ対象へ渡す面積を決める。

プレイ画面では、単にはみ出さないことではなく、そのゲームで考えるために必要な対象を把握できることを基準にする。

PCでは狭い画面を中央へ置いて余白だけを増やすのではなく、ゲーム自体を拡大する、情報配置を変えるなど、広い画面をゲーム体験へ使う。

共通画面ではコンテンツ幅を無制限に広げず、読む・選ぶために適切な密度を保つ。

## 14. 共通UI

共通部品は、デザインシステムを埋めるために作るのではなく、複数画面で同じ意味と振る舞いが確認できたものを実装する。

現在存在する `AppShell`、`PageHeader`、`Button`、`Card` などは再利用できる実装であるが、すべての画面で使用することを正本上の契約にはしない。

特にプレイ画面では、共通部品を維持することより `GAME.md` のゲーム体験を優先する。

フォーム、メニュー、ダイアログ、結果専用部品、通知なども、必要性と共通の意味が確認できてから契約として固定する。

## 15. 避ける表現

次は原則として避ける。

- 共通UIがゲームそのものより強く主張する
- ゲームを汎用アイコンだけで代表させる
- 説明文を増やすことで視覚表現の不足を補う
- パズル固有色をアプリ共通の意味色として再利用する
- すべての情報をカードで囲む
- 共通ヘッダーや共通レイアウトをゲームへ機械的に適用する
- 診断値をプレイヤーへの報酬と同じ強さで並べる
- 常時動く装飾や、意味のない大きな拡縮で注意を奪う
- 画面ごとに似た余白、角丸、影、文字サイズを独自値で増やす
- 将来使うかもしれないという理由だけで共通部品やトークンを追加する

## 16. 変更手順

新しい画面や表現を追加するときは、次の順で判断する。

1. `APP.md` と `GAME.md` から、その画面で守る体験を確認する。
2. この文書から、視覚的優先順位、表現強度、色、面、文字、動きの原則を確認する。
3. 値が必要なら `@theme` の既存トークンを使う。
4. 既存トークンでは別の意味を混同する場合だけ、新しい意味と利用範囲をこの文書へ追加し、正確な値を `@theme` に追加する。
5. 同じ意味と振る舞いが複数箇所にある場合だけ共通UIへ抽出する。
6. ゲームの題材、ルール、識別に由来する表現は各ゲーム配下に残す。
7. スマートフォンとPCの実ブラウザで、情報の優先順位と操作時の見え方を確認する。
