# pa-zzle デザイン

`DESIGN.md` は、pa-zzle のデザイン上の意味・意図・判断原則の正本である。
正確なデザイントークン値は `frontend/styles/globals.css` の Tailwind CSS `@theme` を正本とし、この文書へ重複して記載しない。

## 1. 正本と責務

デザイン判断は次の順で扱う。

1. `DESIGN.md`: 何を主役にし、各表現を何のために使うかを定義する。
2. `frontend/styles/globals.css` の `@theme`: 色、文字、余白、角丸、影、レイヤー、動きなどの正確な値を定義する。
3. `frontend/src/components/`: 複数画面で同じ意味を持つ表現を共通UIとして実装する。
4. 各ゲーム配下: 盤面、駒、色、形状など、そのパズルのルールや識別に必要な表現を実装する。

個別画面の都合だけで新しい共通表現を増やさない。同じ意味が複数箇所で現れたときに共通化する。

## 2. デザイン原則

### パズルを主役にする

アプリ共通の外枠は無彩色を基本とし、彩度の高い色はパズルそのものや、意味上どうしても必要な状態へ優先して使う。ブランド装飾やカードの色で盤面と競合しない。装飾目的のグラデーション、強いぼかし、過度な透過表現は使わず、PoCで成立している平坦で静かな外枠を維持する。

画面で最も強く見えるものは、原則として次の順にする。

1. プレイ中は盤面と現在の操作対象
2. 結果確認では結果と次に行う主要操作
3. 設定・補助操作では現在変更している項目
4. ナビゲーション、説明、問題シードなどの補助情報

### 静かな外枠にする

階層は、色数を増やすのではなく、余白、文字の大きさ・太さ、境界、背景の明度差で作る。影は浮いていることやまとまりを補助するためにだけ使い、装飾目的で重ねない。

### ひとつの局所文脈に主要操作はひとつ

同じ操作群で複数のボタンを同じ強さにしない。次に進む、開始する、結果から再挑戦するなど、その時点で最も自然な操作だけを `primary` とし、やり直し、設定変更、戻るなどは弱い表現にする。

### 状態を色だけで伝えない

選択、エラー、クリアなどは、文字、境界、形状、位置、アイコン、アクセシビリティ属性のうち適切なものを併用する。色の違いが認識できなくても操作状態を判別できることを基準にする。

## 3. 情報設計

### プレイ中

盤面へ視線を集中させる。経過時間、手数、問題情報、Undo、やり直しなどは盤面より弱く表示する。操作説明は初見で必要な範囲に留め、盤面の近くへ置く。

プレイを中断する操作や設定変更は、主要なゲーム操作と視覚的に競合させない。

### 結果確認

結果名を最初に認識できる構造にし、成績は比較しやすい単位でまとめる。次の主要操作を一つ明確にし、再挑戦や難易度変更などは副操作として並べる。

「成功」を緑色だけで表現しない。`クリア` などの明示的な文言と結果構造そのものを成功状態の主表現とする。

### 設定・補助操作

設定項目はラベルと現在値の対応を明確にする。説明文は必要な判断材料だけに絞り、補助情報のために画面密度を上げすぎない。

## 4. 視覚言語

### 色

共通UIでは意味トークンを使う。

- `background`: ページや入力欄などの基底面
- `foreground`: 通常の本文と強い情報
- `card`: 情報をひとまとまりとして扱う面
- `popover`: メニューや選択肢など、一時的に前面へ出る面
- `primary`: 主要操作
- `secondary`: primary より弱い操作
- `muted`: 補助面、補助情報、盤面周囲の静かな背景
- `accent`: hover や選択候補など、一時的な操作反応
- `destructive`: 破壊的操作やエラー
- `border` / `input`: 面や入力領域の境界
- `ring`: キーボードフォーカスや選択対象を示す輪郭

新しい色を共通UIへ直接追加する前に、既存の意味トークンで表せない新しい意味かを確認する。

パズルの色、盤面データから決まる色、駒の識別色はゲーム固有表現であり、共通UIの色トークンへ昇格させない。ウォーターソートの水色などが該当する。

### 背景と面

ページ背景、カード、盤面領域、一時的な前面UIの順に必要な差だけを付ける。すべてをカードで囲わない。カードは「ひとまとまりとして読み、操作する情報」であることを示す場合に使う。

### 文字

文字階層は役割で選ぶ。

- `page-title`: 画面の目的を一度だけ示す最上位見出し
- `section-title`: 結果やまとまりの見出し
- `body`: 読ませる本文
- `supporting`: 説明、戻り導線、補助操作
- `meta`: 問題シード、指標ラベルなど、判断の主対象ではない情報

見出しを大きくするだけで階層を作らず、周囲の余白も合わせて使う。問題シードや数値IDなど固定幅で読む価値があるものだけ `font-mono` を使う。

### 余白

画面外周、セクション間、カード内部、操作群には `@theme` の意味付き余白を使う。同じ役割の余白を画面ごとに微調整しない。

盤面のセル、ボトル、駒などルールや描画寸法に由来する値はゲーム固有値としてよい。共通画面の余白にゲーム固有値を流用しない。

### 角丸・境界・影

- 小さな操作部品は控えめな角丸を使う。
- カードや盤面領域は操作部品より一段大きい角丸を使う。
- 通常の区切りは細い境界、選択対象や盤面形状の強調は必要な場合だけ太い境界を使う。
- 影はカード、入力、popover の階層を補助する最小限にする。
- 境界と影を同時に強くして二重に目立たせない。

### アイコン

アイコンは意味の補助に使い、本文の代わりにしない。装飾アイコンは支援技術から隠す。アイコンだけのボタンには、見た目とは別に操作内容が分かるアクセシブルな名前を必須とする。

ゲーム選択など一覧性が上がる場合はアイコンを使ってよいが、パズル固有の視覚表現より強くしない。

## 5. 操作状態とフィードバック

### 通常・hover・押下

操作可能な要素は通常状態から識別できる。hover はポインター利用者への補助であり、hover しないと意味が分からない設計にしない。

押下は背景や境界などの小さな変化で即時に返す。大きな拡縮や跳ねる動きは使わない。

### フォーカス

キーボードフォーカスは `ring` を使い、背景や境界に埋もれない輪郭を表示する。hover とフォーカスを同一視しない。

### 選択

選択中の対象は輪郭に加え、必要ならラベルや位置変化を併用する。ウォーターソートの注ぎ元・注ぎ先のように役割が異なる場合は、その役割を文字でも確認できるようにする。

### 無効

無効な操作は操作不能にし、視覚的にも弱める。ただし、なぜ無効なのかを理解するために必要なラベルまで消さない。

### エラー

エラーは `destructive` を使えるが、色だけに依存しない。対象との関連が分かる位置に文言を置き、入力など対象部品の `aria-invalid` と整合させる。

### 成功・クリア

クリアは結果名、結果情報、次の操作を明確にした構造で伝える。共通の「成功色」は、他の意味と競合しない必要性が確認できるまで設けない。

## 6. 共通UI

`frontend/src/components/` は、複数画面で同じ意味を持つ最小限の部品を置く。`ui/` に生成済みの部品が存在していても、実利用されていないものをデザインシステム上の確定部品とは扱わない。

現時点で共通利用が確認できている基盤は次のとおり。

- `AppShell`: アプリ全体の幅、ヘッダー、画面余白
- `PageHeader`: 画面タイトル、その前に置く文脈情報、説明の階層
- `Button`: 主要・副次・破壊的操作とアイコン操作
- `Card`: ひとまとまりの情報・操作

フォーム入力、メニュー、ダイアログ、結果専用部品、通知などは、実際に複数箇所で同じ意味と振る舞いが必要になってからデザインシステム上の契約として確定する。

共通部品をゲーム固有の見た目へ寄せない。逆に、ゲーム盤面を共通部品の都合へ合わせない。

## 7. レスポンシブ設計

スマートフォンを基準に情報量と操作順を決め、画面幅が増えたときに余白や列数を増やす。

- ページ外周には共通の横余白を確保する。
- PCではコンテンツ幅に上限を設け、横に広がりすぎない。
- ゲーム盤面は縮小しすぎる前に折り返しや配置変更を検討する。
- 操作群は狭い幅で自然に折り返せるようにする。
- 主なタップ操作は、共通コントロールの標準サイズを使って十分な当たり判定を確保する。小さいサイズは補助的で密度が必要な操作に限定し、主要操作へ使わない。
- 画面幅だけを理由に情報を消さず、優先度に応じて配置を変える。

## 8. アニメーションとトランジション

動きは「操作を受け付けた」「状態が変わった」「前面UIが開閉した」を理解しやすくするために使う。

共通の遷移時間とイージングは `@theme` を使う。個別画面で似た時間を追加しない。常時動く装飾や、操作を待たせる演出は原則として使わない。

`prefers-reduced-motion` を尊重し、意味を失わずに動きを除ける実装にする。位置変化を状態表現に使う場合も、輪郭や文言など静的な手掛かりを残す。

## 9. アクセシビリティ

- 色だけで状態や正誤を伝えない。
- キーボード操作で主要操作へ到達でき、フォーカス位置を視認できるようにする。
- ボタン、リンク、入力には目的が分かるアクセシブルな名前を持たせる。
- 装飾アイコンは読み上げ対象から外す。
- 見出し階層は視覚サイズではなく文書構造と一致させる。
- 文字や操作部品を拡大しても主要情報と操作が失われない構造にする。
- ゲーム固有UIは、盤面の視覚情報だけでなく可能な範囲で状態を支援技術へ伝える。

## 10. 避ける表現

次は原則として避ける。

- 共通UIへ理由なく彩度の高い色を足す
- パズル固有色をブランド色や状態色として再利用する
- 色だけで選択・エラー・クリアを表す
- 画面ごとに似た余白、角丸、影、文字サイズを独自値で増やす
- すべての情報をカードで囲む
- 主要操作を同じ強さで複数並べる
- hover でしか得られない情報を作る
- アイコンだけで意味を推測させる
- 大きな拡縮、跳ね、常時アニメーションでゲーム内容より目立つ
- 将来使うかもしれないという理由だけで共通部品やトークンを追加する

## 11. 変更手順

新しい画面や共通表現を追加するときは、次の順で判断する。

1. この文書の既存原則と意味トークンで表現できるか確認する。
2. 値が必要なら `@theme` の既存トークンを使う。
3. 既存トークンでは別の意味を混同する場合だけ、新しい意味と利用範囲をこの文書へ追加し、正確な値を `@theme` に追加する。
4. 同じ意味と振る舞いが複数箇所にある場合だけ共通UIへ抽出する。
5. ゲームのルールや識別に由来する値ならゲーム配下に残す。
6. PCとスマートフォンの実ブラウザで、見た目、操作、フォーカス、エラーの有無を確認する。
