---
id: SPEC-002
upstream:
  - FR-010
---
# SPEC-002 自己ベスト

自己ベストは専用の保存値ではなく、保存済みの完了プレイ履歴から導出する。

比較単位と比較指標はゲームが定義する。同一の比較単位に過去記録がなければ初記録とする。過去の最良値を厳密に上回った場合だけ自己ベスト更新とし、同値は更新とは扱わない。

同値のプレイが現在のベスト値に該当すること自体は許容する。
